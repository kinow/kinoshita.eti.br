pip install -r requirements.txt
sudo apt install -y latexmk biber unzip texlive-binaries texlive-latex-extra texlive-fonts-extra
sudo mktexlsr

# download from https://www.ctan.org/pkg/moderncv?lang=en
